import React from 'react';
import { Link } from 'react-router-dom';

const Sign = () => {
    return (
        <div>
            <button>Log In</button>
            <button>Sign Up</button>
        </div>
    )
}